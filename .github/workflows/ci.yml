name: CI

# Quality gate only — Render and Vercel already auto-deploy from GitHub on
# their own, so this workflow does not build/push a deploy artifact. It just
# verifies the backend and frontend still install and parse/build cleanly
# before a change merges.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system OCR/PDF deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends tesseract-ocr poppler-utils

      - name: Install Node deps
        run: npm ci

      - name: Install Python deps
        run: pip install -r requirements.txt

      # The live-network collector scripts (server/tests/*.js,
      # server/collectors/tests/) are NOT run here — they hit third-party
      # sites (Unstop, Devfolio) and would flakily fail unrelated PRs. The
      # real Jest suite (server/tests/*.test.js) runs below.
      - name: Syntax-check all backend source files
        run: |
          find . -name '*.js' -not -path './node_modules/*' -print0 \
            | xargs -0 -n1 node -c

      - name: Test
        run: npm test

      - name: Build backend Docker image
        run: docker build -t student-dashboard-server .

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install deps
        run: npm ci

      - name: Test
        run: npm test
        env:
          CI: true

      - name: Build
        run: npm run build
        env:
          CI: true
